from django.apps import AppConfig

class ChicCarryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'  # Optional but recommended
    name = 'chicCarry'  # This must match your app's folder name





